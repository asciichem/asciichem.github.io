---
title: AsciiChem
description: An ASCII syntax for chemistry, model-driven and round-trippable, in Ruby, TypeScript, and Python.
template: splash
hero:
  tagline: ASCII in, chemistry out — in your language.
---

import { Card, CardGrid } from "@astrojs/starlight/components";
import LiveDiff from "../../components/LiveDiff.astro";
import ConformanceMatrix from "../../components/ConformanceMatrix.astro";

<LiveDiff />

AsciiChem is an ASCII syntax for representing **chemistry** — atoms, molecules,
bonds, reactions, electron configurations, and the embedded mathematics
that chemistry publications rely on. It is to chemistry what **AsciiMath** is
to mathematics, but with a semantic model underneath: every parse produces
the same tree, regardless of which front door you came in by.

## One model, three implementations

Three independent packages — `asciichem` (Ruby), `asciichem-ts` (TypeScript),
and `asciichem-py` (Python) — implement the same model and pass the same
[shared corpus](https://github.com/asciichem/asciichem-tests) byte-for-byte.
Pick whichever ships in your stack:

<ConformanceMatrix />

## Why three

A molecule has one structure regardless of the notation you write it in:
InChI, SMILES, molfile, or AsciiChem. Likewise, the *AsciiChem model* has
one shape regardless of the language you run it in. Three implementations
keep the contract honest — no single implementation's shortcuts become the
specification.

## Pick one and start

<CardGrid stagger>
  <Card title="Ruby (reference)" icon="seti:ruby">
    `gem install asciichem` — the reference implementation. MathML,
    structural SVG, citations, InChI identity. See
    [Ruby](/implement/ruby/).
  </Card>
  <Card title="TypeScript" icon="seti:ts">
    `npm install asciichem` — runs in the browser or Node. Powers this
    page's live renderer. See [TypeScript](/implement/typescript/).
  </Card>
  <Card title="Python" icon="seti:python">
    `pip install asciichem-py` — plain dataclasses, zero runtime
    dependencies. See [Python](/implement/python/).
  </Card>
</CardGrid>
